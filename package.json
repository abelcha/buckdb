{
  "version": "0.0.1",
  "name": "buckdb",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "^1.2.5"
  },
  "scripts": {
    "demo": "npm start --prefix demo",
    "patch": "bump --file jsr.json --commit && jsr publish --allow-slow-types --allow-dirty --token $JSR_TOKEN",
    "typecheck": " tsc --noEmit --skipLibCheck --resolveJsonModule --moduleResolution Node --module es2022 --allowSyntheticDefaultImports -p tsconfig.typecheck.json"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.30.0",
    "@duckdb/node-api": "^1.4.1-r.4",
    "es-toolkit": "^1.33.0",
    "typescript": "5.9.2"
  },
  "license": "MIT"
}
